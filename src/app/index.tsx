import { ScreenPlaceholder } from '@/components/screen-placeholder';

// TODO: replace with the responsive cat grid (Requirement 2)
export default function HomeScreen() {
  return <ScreenPlaceholder title="Home" subtitle="Your uploaded cats will live here." />;
}
