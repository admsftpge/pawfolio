import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function UploadScreen() {
  return (
    <ScreenPlaceholder title="Upload" subtitle="Pick a cat photo to add to your collection." />
  );
}
